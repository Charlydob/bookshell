{
  "name": "bookshell-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "22" },
  "main": "src/index.js",
  "scripts": {
    "lint": "node --check src/index.js && node --check src/shortcuts-finance.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:shortcutsApi"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.4.0"
  }
}
